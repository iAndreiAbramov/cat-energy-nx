import { AppRoute } from '@/lib/constants/app-route';

export const LinkText = {
	Slim: 'каталог slim',
	Pro: 'каталог pro',
};

export const LinkHref = {
	Slim: AppRoute.Catalog(),
	Pro: AppRoute.Catalog(),
};
